import { Raleway, Cabin, Catamaran } from "next/font/google";

export const raleway = Raleway({
  variable: "--font-raleway",
  subsets: ["latin"],
});

export const cabin = Cabin({
  variable: "--font-cabin",
  subsets: ["latin"],
});

export const catamaran = Catamaran({
  variable: "--font-catamaran",
  subsets: ["latin"]
})